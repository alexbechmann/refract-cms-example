version: "3"
services:
  app:
    build:
      context: ./
      dockerfile: Dockerfile
    ports: 
      - "5001:5001"
    links:
      - mongo
    environment: 
      - MONGO_CONNECTION_STRING=mongodb://mongo:27017/refract-cms-basic-example